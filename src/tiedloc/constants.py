"""Named constants for the tiedloc simulator.

Avoids magic numbers for element and agent states used throughout the codebase.
"""

# Node / Link states
STATE_OPERATIONAL: int = 0
STATE_FAILED: int = 1

# Server states
STATE_IDLE: int = 0
STATE_BUSY: int = 1
